import 'package:flutter/material.dart';

class StudentNavbar extends StatelessWidget {
  const StudentNavbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Student Navbar'),
      ),
      body: Container(),
    );
  }
}
